name: CI

on:
  push:
    branches: [main, dev, content-sync-hardening]
  pull_request:
    branches: [main, dev]

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: '22.12.0'
          package-manager-cache: false

      - name: Install dependencies
        run: npm install --no-audit --no-fund

      - name: Enforce keyed content contract
        run: npm run content:check

      - name: Content hard tests
        run: npm test

      - name: Astro check
        run: npm run check

      - name: Build
        run: npm run build
